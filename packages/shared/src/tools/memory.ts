import type { SidekickTool } from "./types";

/**
 * memory capability tools. Owned by the memory feature engineer — add
 * `defineTool(...)` entries here; the registry already imports this array.
 */
export const memoryTools: SidekickTool[] = [];
