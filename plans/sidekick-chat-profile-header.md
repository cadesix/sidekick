# Sidekick chat profile header

- Add the selected Sidekick profile identity to the chat sheet header.
- Keep the existing transcript typing indicator as the only typing state.
- Preserve the live 3D phone-holding mascot above the sheet.
- Verify typecheck, lint, and the keyboard-open state in the iOS simulator.
- Compare the simulator capture with the selected visual and record design QA.
- Match the centered three-child header and native `GlassView` chip used by `~/Downloads/imessage-llm`.
