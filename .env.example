# Optional: enables real AI chat replies (otherwise the app uses canned replies).
# For local dev only — a bundled key ships in the JS. For production, put a proxy
# in front (e.g. the sans tRPC backend) and call that instead.
EXPO_PUBLIC_OPENAI_API_KEY=
